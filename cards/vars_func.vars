package main

import (
	"fmt"
)

// Variable declaration use := and equal sign for assignment
// Variable assignment need to be within functions, declarations can be outside

func main() {
	// One := 1
	// Two := 2

	// card := newCard(One, Two)
	card := newCard()
	fmt.Println(card)
}

// Need to be explicit with return type  if returning values or accepting values.
// Python requires input values only.
// func newCard(int, int) string {
func newCard() string {
	return "Five of Diamonds"
}
